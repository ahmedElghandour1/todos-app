export const element = (selector, parentNode) => {
        return (parentNode || document).querySelector(selector);

} 
export const elements = (selector, parentNode) => {
        return (parentNode || document).querySelector(selector);
} 